/// @description 여기에 설명 삽입
// 이 에디터에 코드를 작성할 수 있습니다

if(array_length(global.player_body_vertex) != 0)
{
	mine = global.player_body_vertex[array_length(global.player_body_vertex)-1];
	array_push(global.player_body_vertex,id)
}
else
{
	array_push(global.player_body_vertex,id)
}

// Inherit the parent event
event_inherited();

